import type { ToolDef } from '@/types'

/**
 * AI tools shown in the Tool Explorer (Interaction D) and the tool scenes.
 *
 * GAP: the original presentation's tool list has not been supplied yet.
 * Which tools are named — and how their limitations are described — is
 * educational content, so it is left empty rather than invented.
 * Populate from the deck before Task 15 (AI Tool Explorer).
 */
export const TOOLS: readonly ToolDef[] = [] as const
